<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <title>Document</title>
</head>
<body>
  <nav>
    <div class="container">
      <div class="nav-wrapper">
        <a href="#" class="brand-logo center">Coba-Coba</a>
      </div>
    </div>
  </nav>
  
  <div class="container">
    <h3>Edit Item: <%= item.name %></h3>
    <form class="col s12" action="/items/<%=item.id%>/update" method="POST">
      <div class="row">
        <div class="input-field col s6">
          <input placeholder="input item's name." id="name" type="text" class="validate" name="name" value="<%=item.name%>">
          <label for="name">Name</label>
        </div>
        <div class="input-field col s6">
          <input placeholder="input item's price." id="price" type="number"   class="validate" name="price" value="<%=item.price%>">
          <label for="price">Price</label>
        </div>
      </div>

      <div class="row">
        <label>Category</label>
        <select class="browser-default" name="category_id">
          <option value="" disabled>Select item's category</option>
          <% categories.each do |category| %>
            <% if category.id == item.category.id %>\
              <option value="<%= category.id %>" selected><%= category.name %></option>
            <% else %>
              <option value="<%= category.id %>"><%= category.name %></option>
            <% end %>
          <% end %>
        </select>
      </div>
      <div class="row">
        <button class="btn waves-effect waves-light" type="submit" name="action">Update Item
          <i class="material-icons right">send</i>
        </button>
      </div>  
    </form>
  </div>
  <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

</body>
</html>